import { FIREBASE_CONFIG } from './firebase.key';
import { RIOT_TOKEN } from '../../../loi-des-norms-server/app/res/riot-key.key';

export const environment = {
  production: true,
  firebase: FIREBASE_CONFIG,
};
